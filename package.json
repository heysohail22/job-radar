{
  "name": "job-radar",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered job discovery radar and instant ATS resume tailor",
  "scripts": {
    "dev": "pnpm --prefix frontend dev",
    "build": "pnpm --prefix frontend build",
    "start": "pnpm --prefix frontend start",
    "lint": "pnpm --prefix frontend lint",
    "backend": "cd backend && uv run uvicorn main:app --reload --port 8000"
  },
  "packageManager": "pnpm@12.5.1"
}
